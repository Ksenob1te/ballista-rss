services:
  ballista-rss:
    build:
      context: .
    env_file:
      - .env.prod
    restart: unless-stopped
    networks:
      - default
      - compose_rabbitmq

networks:
  default:
    driver: bridge
  compose_rabbitmq:
    external: true